# TODO

## Идеи

- **Callback-метод**: принимать callback от eXpress; хранилище данных — очередь / файлы / локальная БД; возможно, псевдо-синхронный режим (ждать callback после отправки)
- **Команда `api`**: отправка произвольных запросов к eXpress API (`express-botx api POST /path ...`)
